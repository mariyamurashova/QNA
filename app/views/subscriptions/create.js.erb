$('.errors').html('<%= render 'shared/errors', resource: @subscription %>');

<% if @subscription.persisted? %>
$('.notice').html(' ');
$('.notice').html('<%= flash[:notice] %>');
$('.question_unsubscribe').removeClass('hidden');
$('.question_unsubscribe').html('<%= render 'subscriptions/delete_btn', resource: @subscription.question %>')
$('#subscribe_question').addClass('hidden');
<%end%>

